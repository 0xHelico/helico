# Copy to .env (gitignored). bun loads it automatically for the smoke and e2e scripts.
# Test wallet for `bun run e2e` on Base Sepolia. Use a burner that holds testnet ETH and Circle test USDC.
PRIVATE_KEY=
# Optional RPC override for `smoke` (Base mainnet) and `e2e` (Base Sepolia).
RPC_URL=
