# Upload half of `secrets.yaml`, not a manifest. See that file for why two exist.
# The model router's three credentials.
secretsNames:
    AI_USERNAME:
        - SECRET_AI_USERNAME
    AI_PASSWORD:
        - SECRET_AI_PASSWORD
    AI_API_KEY:
        - SECRET_AI_API_KEY
